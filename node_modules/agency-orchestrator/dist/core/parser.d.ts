import type { WorkflowDefinition } from '../types.js';
export declare function parseWorkflow(filePath: string): WorkflowDefinition;
/**
 * 验证工作流定义（不执行），返回错误列表
 */
export declare function validateWorkflow(workflow: WorkflowDefinition): string[];
