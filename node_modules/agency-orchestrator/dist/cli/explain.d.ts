/**
 * ao explain — 用自然语言解释 workflow 的 DAG 执行计划
 */
import type { WorkflowDefinition } from '../types.js';
/**
 * 生成 workflow 的自然语言解释
 */
export declare function explainWorkflow(workflow: Pick<WorkflowDefinition, 'name' | 'steps' | 'inputs'>): string;
