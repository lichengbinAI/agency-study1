/**
 * ao run --watch — 终端实时进度显示
 */
export interface ProgressEvent {
    type: 'step_start' | 'step_done' | 'step_skip' | 'step_error';
    stepId: string;
    role: string;
    elapsed?: number;
    total: number;
    completed: number;
}
export type ProgressCallback = (event: ProgressEvent) => void;
/**
 * 创建 watch 渲染器，返回 ProgressCallback
 * @param workflowName 工作流名称
 * @param stepIds 全部步骤 id 列表
 * @param roles 全部步骤角色列表
 */
export declare function createWatchRenderer(workflowName: string, stepIds: string[], roles: string[]): ProgressCallback;
